# lx-serv

Installer GitHub CLI
sudo apt install gh

Authentifier le serveur
gh auth login


Uninstall globalement 
Faut aller supprimer les allows user dans sshd config 
+ supprimer le shell dans /etc/shells